# Python / Django
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
*.sqlite3

# Entorno y secretos
.env
.env.*
!.env.example

# Estáticos / media subidos
/staticfiles/
/media/

# Editor / SO
.vscode/
.idea/
.DS_Store
Thumbs.db

# Herramienta de túnel temporal (binario pesado, no va al repo)
cloudflared.exe
# Build del frontend (se genera al desplegar)
/frontend/dist/

# Datos reales de pacientes (nombre + psicologo + sesion): NUNCA al repo.
datos_pacientes_reales.json

# Exportables y auditorías del Centro de Continuidad (pueden llevar PII): NUNCA al repo.
auditorias_continuidad/
auditorias_continuidad_notas/
revision_continuidad_*.csv
revision_continuidad_*.txt
