from django.apps import AppConfig


class EspaciosConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "espacios"
    verbose_name = "Espacios profesionales (alquiler)"
